.env
secrets.local.env
.coverage
node_modules
.claude/

# Local secrets — never commit
secrets.local.env

# CDK synthesized output — regenerated on every cdk synth
cdk.out/

service/infra/dist

aws-agentcore
.vercel

*env*